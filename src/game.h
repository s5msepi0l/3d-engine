#ifndef GAME_MAIN
#define GAME_MAIN

#include "util.h"
#include "renderer.h"
#include <cmath>

namespace game {
    typedef struct {
        struct world { // world and whatnot

        };
    }state;


    f64 raycast(f32 x , f32 y, f32 angle, f32 max_depth) {
        f32 sin_a = sin(angle);
        f32 cos_a = cos(angle);

        for (i32 i = 1; i<)
    }

    class game {
        private:
            keyboard_input input;

        public:
            void run() {

            }

        private:


    };


}

#endif